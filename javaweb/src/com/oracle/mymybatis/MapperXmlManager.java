package com.oracle.mymybatis;

import javax.swing.text.Document;
import java.util.HashMap;
import java.util.Map;

public class MapperXmlManager {
    private static Map<String,String> sqlMap=new HashMap<>();
    static {
        String[] xmlPath=MyMyBatisXmlManager.getXmlPath();
        for(String path:xmlPath){
            SAXBuilder
            Document document=sb.
        }
    }
}
